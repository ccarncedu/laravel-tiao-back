```
Iniciar container
docker-compose up --build -d

Parar container 
docker-compose down
```

```
É necessário criar a key, no Google Cloud Console

Indo em biblioteca, e gerando a key para YouTube Data API v3 e colocando na ENV, o seu valor em YOUTUBE_API_KEY em docker-compose yml
```